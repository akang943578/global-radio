diff --git a/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionPlugin.java b/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionPlugin.java
index 6f54b7f..dfb3ef5 100644
--- a/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionPlugin.java
+++ b/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionPlugin.java
@@ -82,9 +82,28 @@ public class MediaSessionPlugin extends Plugin {
     }
 
     public void startMediaService() {
+        // Patched: wrap in try/catch. Android 14 can throw
+        // ForegroundServiceStartNotAllowedException if the app is briefly
+        // considered "not in foreground" (e.g. WebView still transitioning),
+        // and the original code would let the exception escape and kill the
+        // play action entirely.
         Intent intent = new Intent(getActivity(), MediaSessionService.class);
-        ContextCompat.startForegroundService(getContext(), intent);
-        getContext().bindService(intent, serviceConnection, Context.BIND_AUTO_CREATE);
+        try {
+            ContextCompat.startForegroundService(getContext(), intent);
+        } catch (Exception e) {
+            Log.w(TAG, "startForegroundService failed, falling back to startService", e);
+            try {
+                getContext().startService(intent);
+            } catch (Exception ignored) {
+                Log.w(TAG, "startService also failed", ignored);
+                return;
+            }
+        }
+        try {
+            getContext().bindService(intent, serviceConnection, Context.BIND_AUTO_CREATE);
+        } catch (Exception e) {
+            Log.w(TAG, "bindService failed", e);
+        }
     }
 
     private void updateServiceMetadata() {
@@ -97,6 +116,15 @@ public class MediaSessionPlugin extends Plugin {
 
 
     private Bitmap urlToBitmap(String url) throws IOException {
+        // Patched: ABORT for http(s) URLs. The original implementation does a
+        // *synchronous* HttpURLConnection.connect() on the main thread with no
+        // timeout, which freezes the bridge call (and thus the @PluginMethod)
+        // for as long as the favicon server takes — potentially forever.
+        // When that times out / throws, setMetadata propagates the IOException
+        // back to JS, the follow-up setPlaybackState lands too late for
+        // Android 14's startForeground window, and the MediaStyle card never
+        // appears in the shade. We disable the http path here; base64 data
+        // URIs are still supported (no network, decodes in-process).
         final boolean blobUrl = url.startsWith("blob:");
         if (blobUrl) {
             Log.i(TAG, "Converting Blob URLs to Bitmap for media artwork is not yet supported");
@@ -104,11 +132,8 @@ public class MediaSessionPlugin extends Plugin {
 
         final boolean httpUrl = url.startsWith("http");
         if (httpUrl) {
-            HttpURLConnection connection = (HttpURLConnection) (new URL(url)).openConnection();
-            connection.setDoInput(true);
-            connection.connect();
-            InputStream inputStream = connection.getInputStream();
-            return BitmapFactory.decodeStream(inputStream);
+            Log.i(TAG, "Skipping http(s) artwork fetch (patched to avoid main-thread sync IO)");
+            return null;
         }
 
         int base64Index = url.indexOf(";base64,");
@@ -127,14 +152,31 @@ public class MediaSessionPlugin extends Plugin {
         artist = call.getString("artist", artist);
         album = call.getString("album", album);
 
+        // Patched: reset artwork on every setMetadata call. The original code
+        // only updated this.artwork inside the loop, so if JS sent an empty
+        // artwork: [] (e.g. switching to a station with no favicon), the
+        // previous station's bitmap would leak onto the new card. Now an
+        // empty / missing array explicitly clears the artwork.
         final JSArray artworkArray = call.getArray("artwork");
-        final List<JSONObject> artworkList = artworkArray.toList();
-        for (JSONObject artwork : artworkList) {
-            String src = artwork.getString("src");
-            if (src != null) {
-                this.artwork = urlToBitmap(src);
+        Bitmap nextArtwork = null;
+        if (artworkArray != null) {
+            final List<JSONObject> artworkList = artworkArray.toList();
+            for (JSONObject artwork : artworkList) {
+                String src = artwork.optString("src", null);
+                if (src == null || src.isEmpty()) continue;
+                try {
+                    Bitmap bmp = urlToBitmap(src);
+                    if (bmp != null) {
+                        nextArtwork = bmp;
+                        break; // 第一张能解码的就用，剩下的不浪费 CPU
+                    }
+                } catch (Exception e) {
+                    // 单张图解不出来不该把整个 setMetadata 干挂；继续看下一张
+                    Log.d(TAG, "urlToBitmap failed for one artwork entry, skipping", e);
+                }
             }
         }
+        this.artwork = nextArtwork;
 
         if (service != null) { updateServiceMetadata(); };
         call.resolve();
diff --git a/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionService.java b/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionService.java
index cb0417f..e397b10 100644
--- a/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionService.java
+++ b/node_modules/@jofr/capacitor-media-session/android/src/main/java/io/github/jofr/capacitor/mediasessionplugin/MediaSessionService.java
@@ -1,6 +1,7 @@
 package io.github.jofr.capacitor.mediasessionplugin;
 
 import android.annotation.SuppressLint;
+import android.app.Notification;
 import android.app.NotificationChannel;
 import android.app.NotificationManager;
 import android.app.PendingIntent;
@@ -60,6 +61,7 @@ public class MediaSessionService extends Service {
     private MediaSessionCallback callback;
 
     private final IBinder binder = new LocalBinder();
+    private boolean foregroundStarted = false;
 
     public final class LocalBinder extends Binder {
         MediaSessionService getService() {
@@ -79,6 +81,51 @@ public class MediaSessionService extends Service {
         return super.onUnbind(intent);
     }
 
+    @Override
+    public void onCreate() {
+        super.onCreate();
+        // Pre-create the notification channel so it shows up in the system
+        // notification settings the moment the service starts, regardless of
+        // whether connectAndInitialize() has had a chance to run.
+        notificationManager = (NotificationManager) getSystemService(NOTIFICATION_SERVICE);
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O && notificationManager != null) {
+            NotificationChannel channel = new NotificationChannel(
+                "playback", "Playback", NotificationManager.IMPORTANCE_LOW);
+            notificationManager.createNotificationChannel(channel);
+        }
+    }
+
+    /**
+     * Patched: call startForeground immediately in onStartCommand with a
+     * placeholder notification. Android 14 (API 34) gives a service only
+     * 5 seconds from startForegroundService() to startForeground(),
+     * otherwise throws ForegroundServiceDidNotStartInTimeException. The
+     * original code relied on connectAndInitialize() (triggered by
+     * bindService → onServiceConnected) to call startForeground, which
+     * can lose the race on slow devices or under load.
+     */
+    private void ensureForegroundStarted() {
+        if (foregroundStarted) return;
+        try {
+            NotificationCompat.Builder placeholderBuilder =
+                new NotificationCompat.Builder(this, "playback")
+                    .setSmallIcon(R.drawable.ic_baseline_volume_up_24)
+                    .setContentTitle("Playback")
+                    .setOngoing(true)
+                    .setVisibility(NotificationCompat.VISIBILITY_PUBLIC);
+            Notification placeholder = placeholderBuilder.build();
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+                startForeground(NOTIFICATION_ID, placeholder,
+                    ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK);
+            } else {
+                startForeground(NOTIFICATION_ID, placeholder);
+            }
+            foregroundStarted = true;
+        } catch (Exception e) {
+            Log.w(TAG, "ensureForegroundStarted failed", e);
+        }
+    }
+
     public void connectAndInitialize(MediaSessionPlugin plugin, Intent intent) {
         this.plugin = plugin;
 
@@ -95,10 +142,10 @@ public class MediaSessionService extends Service {
                 .putLong(MediaMetadataCompat.METADATA_KEY_DURATION, duration);
         mediaSession.setMetadata(mediaMetadataBuilder.build());
 
-        notificationManager = (NotificationManager) getSystemService(NOTIFICATION_SERVICE);
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-            NotificationChannel channel = new NotificationChannel("playback", "Playback", NotificationManager.IMPORTANCE_LOW);
-            notificationManager.createNotificationChannel(channel);
+        // Patched: notificationManager + channel are now created in onCreate(),
+        // so they are guaranteed to exist before any startForeground call.
+        if (notificationManager == null) {
+            notificationManager = (NotificationManager) getSystemService(NOTIFICATION_SERVICE);
         }
 
         notificationStyle = new MediaStyle().setMediaSession(mediaSession.getSessionToken());
@@ -106,12 +153,34 @@ public class MediaSessionService extends Service {
                 .setStyle(notificationStyle)
                 .setSmallIcon(R.drawable.ic_baseline_volume_up_24)
                 .setContentIntent(PendingIntent.getActivity(getApplicationContext(), 0, intent, PendingIntent.FLAG_IMMUTABLE))
-                .setVisibility(NotificationCompat.VISIBILITY_PUBLIC);
-
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
-          startForeground(NOTIFICATION_ID, notificationBuilder.build(), ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK);
-        } else {
-          startForeground(NOTIFICATION_ID, notificationBuilder.build());
+                .setVisibility(NotificationCompat.VISIBILITY_PUBLIC)
+                // Patched: keep the notification "ongoing" regardless of play/pause
+                // state. MIUI/HyperOS treats non-ongoing notifications as
+                // dismissable and aggressively prunes them — once swiped or
+                // pruned, NotificationManager.notify() can't bring them back.
+                // Always-ongoing + re-startForeground (see updateNotification
+                // below) is the supported way to keep the MediaStyle card pinned.
+                .setOngoing(true);
+
+        // Patched: prefer startForeground() over notificationManager.notify()
+        // for posting / replacing the rich MediaStyle notification. On stock
+        // AOSP it's a no-op once we're foreground; on MIUI/HyperOS the only
+        // reliable way to (re)mount the ongoing notification slot is to call
+        // startForeground each time. notify() alone gets dropped silently.
+        Notification initialNotification = notificationBuilder.build();
+        try {
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+                startForeground(NOTIFICATION_ID, initialNotification,
+                    ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK);
+            } else {
+                startForeground(NOTIFICATION_ID, initialNotification);
+            }
+            foregroundStarted = true;
+        } catch (Exception e) {
+            Log.w(TAG, "startForeground in connectAndInitialize failed; falling back to notify()", e);
+            if (notificationManager != null) {
+                notificationManager.notify(NOTIFICATION_ID, initialNotification);
+            }
         }
 
         notificationActions.put("play", new NotificationCompat.Action(
@@ -153,7 +222,16 @@ public class MediaSessionService extends Service {
 
     @Override
     public int onStartCommand(Intent intent, int flags, int startId) {
-        MediaButtonReceiver.handleIntent(mediaSession, intent);
+        // Patched: call startForeground IMMEDIATELY to satisfy Android 14's
+        // 5-second startForegroundService → startForeground window. The
+        // original code only called startForeground from connectAndInitialize,
+        // which is triggered asynchronously via bindService → onServiceConnected
+        // and could lose the race, killing the service before any media
+        // notification was posted.
+        ensureForegroundStarted();
+        if (mediaSession != null) {
+            MediaButtonReceiver.handleIntent(mediaSession, intent);
+        }
         return super.onStartCommand(intent, flags, startId);
     }
 
@@ -290,8 +368,39 @@ public class MediaSessionService extends Service {
             notificationBuilder
                     .setContentTitle(title)
                     .setContentText(artist + " - " + album)
-                    .setLargeIcon(artwork);
-            notificationManager.notify(NOTIFICATION_ID, notificationBuilder.build());
+                    .setLargeIcon(artwork)
+                    // Always ongoing — see comment in connectAndInitialize.
+                    .setOngoing(true);
+            // Patched (v2.0.21): on every metadata/state update, re-call
+            // startForeground(NOTIFICATION_ID, notif, type) instead of plain
+            // notificationManager.notify(...). On stock AOSP this is a no-op
+            // (we are already foreground), but on MIUI/HyperOS the system
+            // aggressively dismisses the ongoing media notification on
+            // pause→resume / station-change cycles, and notify() alone won't
+            // bring it back. Re-calling startForeground forces the system
+            // back into the foreground-notification slot, which is the only
+            // reliable way to keep the MediaStyle card visible on Xiaomi.
+            // Fall back to notify() only if startForeground throws (e.g. the
+            // service somehow isn't allowed to be foreground anymore — at
+            // least we don't lose the notification entirely).
+            Notification notif = notificationBuilder.build();
+            boolean delivered = false;
+            if (foregroundStarted) {
+                try {
+                    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+                        startForeground(NOTIFICATION_ID, notif,
+                                ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK);
+                    } else {
+                        startForeground(NOTIFICATION_ID, notif);
+                    }
+                    delivered = true;
+                } catch (Exception e) {
+                    Log.w(TAG, "Re-startForeground failed; falling back to notify()", e);
+                }
+            }
+            if (!delivered && notificationManager != null) {
+                notificationManager.notify(NOTIFICATION_ID, notif);
+            }
             notificationUpdate = false;
         }
     }
